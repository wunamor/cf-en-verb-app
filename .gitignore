# --- 依赖包 (绝对不能上传) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # 可选：通常建议上传以锁定版本，但如果你不熟悉，也可以忽略
yarn.lock         # 同上

# --- Cloudflare Workers 特定文件 ---
.wrangler/        # Wrangler 本地开发生成的临时文件
dist/             # 构建输出目录
worker/           # 构建输出目录
.dev.vars         # 【重要】本地开发的环境变量（包含你的 ADMIN_PASSWORD）

# --- 环境变量与密钥 (安全防线) ---
.env
.env.local
.env.*.local

# --- 操作系统生成的文件 ---
.DS_Store         # macOS
Thumbs.db         # Windows
ehthumbs.db
Desktop.ini

# --- IDE 编辑器配置 (可选，建议忽略个人配置) ---
.idea/
.vscode/
*.swp
*.swo